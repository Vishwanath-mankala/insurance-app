import { DashboardStats } from './dashboard-stats';

describe('DashboardStats', () => {
  it('should create an instance', () => {
    expect(new DashboardStats(0, 0, 0, 0, 0, 0)).toBeTruthy();
  });
});
